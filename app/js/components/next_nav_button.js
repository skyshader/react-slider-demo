import React from "react";

export default class NextNavButton extends React.Component {
    render() {
        return <button className="slick-arrow slick-next" {...this.props}>Next</button>
    }
}
